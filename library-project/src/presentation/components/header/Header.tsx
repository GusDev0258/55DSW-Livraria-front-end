import React from 'react'

import { StyledHeader } from '../../styles/styled-components/header/styled-header'

const Header = () => {

  return (
    <StyledHeader>
      Library
    </StyledHeader>
  )
}

export default Header